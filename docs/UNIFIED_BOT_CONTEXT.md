# Unified Algo Trading Bot — Full Project Context & Architecture

**Status:** Phase 2 Demo Testing — Week 4 (Aug 11–15 2026)
**Last Updated:** Aug 9 2026
**Environment:** Exness MT5 Demo — XAUUSDm · EURUSDm · GBPUSDm ·
XAGUSDm · BTCUSDm · ETHUSDm · USTECm · USOILm · USDJPYm · EURGBPm · AUDNZDm
**Account:** 474167713 · Exness-MT5Trial15
**Lot Size:** 0.01 (demo phase — hard-capped via `cab_max_lot_demo_cap`)
**Codebase:** ~9,200 lines Python + MQL5 across 18 files

---

## 1. Project Philosophy

This is **not** a universal trading system. Three specific strategies were
devised through a dedicated research process and shortlisted from a larger
candidate pool:

1. **Ghost Grid** — Mean-reversion accumulation (ranging/stable regimes)
2. **SuperTrend** — Trend-following via K-Means optimal factor (trending regimes)
3. **CAB Watcher** — H4 inversion detection (exhaustion/reversal regimes)

Each strategy could run independently, but they run together as a unified
process for a specific reason: **markets operate in regimes** (ranging,
trending, exhaustion). One model thrives in ranging conditions while another
thrives in trending conditions. By running them together and sharing market
knowledge through the Knowledge Register, each bot can potentially help the
others achieve collective profitability across all market states.

**Current Phase: Raw Edge Validation**
Before introducing any filters, restrictions, or optimizations, the system
must first validate whether a raw mathematical edge exists. Every signal
generated by the bots must flow unobstructed to the market so that the
resulting data can be analyzed to determine:
- Which pairs each bot performs best on
- Which regimes produce winners vs losers
- Whether the shared-knowledge hypothesis holds
- What statistically-informed filters (if any) should be introduced

**[PERMANENT MANDATE] Knowledge Register Purpose:**
The Knowledge Register exists strictly to gather and serialize raw market
state information alongside trade outcomes. It must NEVER be used to
arbitrarily block, filter, or restrict trades. Any future filters must be
statistically derived from the raw data analysis — mapping why trades win
vs. lose in specific market behaviors/regimes — not from human intuition
or preference.

**Core principle (finalised May 2026):**
Every exit decision must be driven by market intelligence, not rigid
R-thresholds. R-multiples measure what happened. Market state tells you
what is happening.

**Exit intelligence roadmap (Market Exit Score, Phase 3):**
After raw edge validation, static R-threshold exits will be replaced
or supplemented by a continuous Market Exit Score (MES, 0-1) computed from:
regime alignment (30%), conviction trend (25%), ATR trajectory (20%),
directional momentum (15%), opposite signal quality (10%). MES < 0.30
exits regardless of R level.

**The finish line — Gate 6:**
One unified process, micro-lot sizing, avg R > 0.3 over 50 live trades on
validated bot-instrument pairs.

---

## 2. Regime Complementarity Map

| Regime | ADX Signal | SuperTrend | Ghost Grid | CAB Watcher |
|---|---|---|---|---|
| **RANGING** | ADX low | SLEEPING | **ACTIVE 201/202** | Watching |
| **STABLE** | ADX mid | Watching | 201 only | Watching |
| **TRENDING** | ADX 55+ | **ACTIVE** | 202 BLOCKED | REAPER alert |
| **EXHAUSTION** | ATR spike, weak body | Exits via DEAD state | 202 fading | **HARVESTER fires** |

**Probe arming gate (added Jul 2026):** Ghost Grid will not arm a new probe
during TRENDING_UP, TRENDING_DOWN, or UNKNOWN regime. Even though individual
leg gates (202) were regime-aware, the arming decision itself had no
check before this fix. Implemented via `GHOST_ARM_BLOCKED_REGIMES` in
`unified_runner.py`.

---

## 3. File Structure & Archive Prevention Mandate

> [!WARNING]
> **STRICT ARCHIVE PREVENTION MANDATE**
> NO code file (`.py`), test file (`test_*.py`), configuration file (`.json`), or documentation file is EVER to be moved out of its designated folder into an `archive/` or `archive_*/` directory. Moving files causes catastrophic `ModuleNotFoundError` crashes and breaks test suites. If a file is deprecated, flag it in code or remove it only after explicit user authorisation. NEVER arbitrarily "clean up" the root by moving live files.

```
Super/
├── unified_runner.py               MASTER ENTRY POINT — 5 daemon threads
├── mt5_gateway.py                  Thread-safe MT5 wrapper (single Lock)
├── config/
│   └── config.json                 Credentials, symbols, caps, live switches
├── core/
│   ├── __init__.py
│   ├── knowledge_register.py       Global state and invalidation bus
│   └── supertrend_bot.py           SuperTrend v2.3 — M30 trend following
├── ghost_super/
│   ├── __init__.py
│   ├── ghost_sniper.py             Ghost Hunter v5.1 — probe detect + entry
│   ├── sniper_watcher.py           Ghost Brain v3.1 — market pulse + grid mgmt
│   └── ghost_cache.py              GhostCache — virtual grid layer tracker (204)
├── cab_super/
│   ├── __init__.py
│   ├── cab_entry.py                CAB Entry Engine — H4 inversion detector
│   └── cab_watcher.py              CAB Watcher v16.3 — fluid position manager
├── UnifiedFailover.mq5             MQL5 EA — 9 magic numbers, 60s/120s failover
├── verify_structure.py             Structure and import health check
├── test_cab_entry.py               18 tests for CAB entry engine (DO NOT MOVE)
├── test_cab_gateway_port.py        10 tests for CAB gateway port (DO NOT MOVE)
├── test_ghost_gateway_port.py      17 tests for Ghost gateway port (DO NOT MOVE)
└── test_supertrend_gateway_port.py 12 tests for ST gateway port (DO NOT MOVE)
```

---

## 4. Magic Number Registry

| Bot | Magic | Symbol | Purpose |
|---|---|---|---|
| SuperTrend | 101234 | EURUSDm | M30 trend entry |
| SuperTrend | 201567 | GBPUSDm | M30 trend entry |
| SuperTrend | 301890 | XAUUSDm | M30 trend entry |
| SuperTrend | 401213 | XAGUSDm | M30 trend entry |
| Ghost Grid | 201 | XAUUSDm | SCALP leg |
| Ghost Grid | 202 | XAUUSDm | REVERSAL leg |
| Ghost Grid | 203 | -- | REMOVED — shifted to SuperTrend |
| Ghost Grid | 204 | XAUUSDm | GHOST CACHE (nth virtual layer) |
| CAB Watcher | 999555 | All pairs | H4 inversion positions |

Isolation check runs every 5 minutes in the main thread. Breaches log as
`MAGIC_ISOLATION_BREACH_DETAIL` with the actual magic number and symbol in
the structured CSV (added Jul 2026).

**Week 1 isolation breach investigation — resolved:** 31 isolation breaches
were logged in Week 1 with comment tags `WATCHER_BUY`, `SENTINEL_INVALID`,
`PARTIAL_SCALE`, `SAFETY_NO_SL_KIL` on USDJPYm and EURGBPm positions. These
pairs ARE present in config.json (magic 406213 and 407213 respectively) and
were trading legitimately. The breach tags indicated unrecognised comment
strings from early bot versions, not an external actor. The
`MAGIC_ISOLATION_BREACH_DETAIL` logging added in Jul 2026 confirmed this.
No external trading was occurring.

---

## 5. Thread Architecture

```
Main thread (60s)       equity heartbeat, circuit breaker, magic isolation, MT5 health
Thread 1 GhostWatcher   market pulse ADX/ATR/conviction → SharedState (2s pulse)
Thread 2 GhostHunter    M1 probe detection → fires 201/202/204 (1s loop)
Thread 3 CABWatcher     manages open CAB positions fluid matrix (15s cycle)
Thread 4 CABEntry       H4 inversion detection → fires 999555 (30s cycle)
Thread 5 SuperTrend     M30 trend following → fires 11 pairs (101234–408213) (30s cycle)
```

All threads share one MT5Gateway instance. The gateway threading.Lock
serialises every MT5 API call. One connection, one lock, no race conditions.

---

## 6. Bot Details — Ghost Grid

**Strategy:** Virtual ghost grid accumulates in memory against a probe move.
Fires real entry at exhaustion point. Mean-reversion philosophy — active in
RANGING/STABLE only. Primary instrument: XAUUSDm.

### Entry legs

| Magic | Leg | Gate | TP | SL |
|---|---|---|---|---|
| 201 | SCALP | Always | 1.0R | 0.35xATR |
| 202 | REVERSAL | ADX <= 40 AND conviction <= 60 | 1.5R | 0.35xATR |
| 203 | TREND-FOLLOW | **REMOVED** (Shifted to SuperTrend) | -- | -- |
| 204 | GHOST CACHE | nth virtual layer reached | 2.0R | nth_layer +/- 0.5xATR |

SL note: Was 0.2xATR — confirmed from 2-week data as the single largest
loss driver (41.5% of 201/202 trades hit SL before reaching BE-lock,
-$2,559.67). Widened to 0.35xATR in Jul 2026. Both the live thread in
unified_runner.py and ghost_sniper.py now read SCALP_REV_SL_ATR_MULT = 0.35.

### Grid management thresholds

| Threshold | Value | Action |
|---|---|---|
| Grid TP | combined R >= +1.0R | Close all remaining legs, reset GridState |
| Grid Stop | combined R <= -3.0R | Close all immediately |
| Grid Protector | combined R >= +0.35R | Oldest leg SL to BE + 30pts, once per grid |
| BE-lock ratchet | individual R >= +1.0R | SL to breakeven + 30pts (one-time per leg) |
| Max grid age | 72 hours | Force-close and reset stale timeout safety valve |
| MAX_LEGS | 4 | Hard cap — never raise during demo testing |

### Ghost Cache (magic 204)

```
N_LAYERS_DEFAULT       = 3      start here, do not change until 30+ fills
SL_ATR_BUFFER          = 0.5    204 SL = nth_layer +/- 0.5xATR
CACHE_INVALIDATION_N   = 2      reaches nth+2 layer = trend, invalidate cache
MAX_CACHE_AGE_SECONDS  = 300    5 min — ATR step goes stale after this
```

Gate 2 decision matrix (after 30+ fills each):
- 204 avg_R > 202 avg_R by >20% → replace 202 with 204 in Phase 4
- 204 approximately equal to 202 (within 20%) → keep both, tune by regime
- 204 avg_R < 202 avg_R → kill 204, 202 confirmed
- 204 fires fewer than 10 times in 3 weeks → lower n to 2
- 204 fires more than 6 times per day → raise n to 4

**204 fired ZERO times in Week 1** — wiring bug: GhostCache was never
instantiated in the live unified_runner.py thread (only in the standalone
ghost_sniper.py reference). Fixed Jul 2026. Week 2 is the first real
data collection window for Gate 2.

### Key constants (ghost_sniper.py)
```
SCALP_REV_SL_ATR_MULT  = 0.35
EMERGENCY_TP_ATR_MULT  = 3.0
ATR_STEP_MULTIPLIER    = 1.0
ATR_STEP_FLOOR         = 0.5
SPREAD_MAX             = 1.5
COOLDOWN_SECONDS       = 45
GATE_202_ADX_BLOCK     = 40
GATE_202_CONV_BLOCK    = 60
# 203 thresholds removed — handled entirely by SuperTrend
```

### Key constants (sniper_watcher.py)
```
GRID_TP_R              = 1.0
GRID_STOP_R            = -3.0
GRID_PROT_R            = 0.35
BE_LOCK_R              = 0.5
BE_LOCK_BUFFER         = 30    points
MAX_GRID_AGE_SECONDS   = 259200  72 hours
LEG_TP_BY_MAGIC        = 201:1.0, 202:1.5, 204:2.0
REAPER_ATR_MULT_201    = 1.5
# 203 trails removed — handled entirely by SuperTrend
TREND_ADX_THRESH       = 65
EXHAUST_ATR_THRESH     = 75
EXHAUST_BODY_THRESH    = 35
REGIME_BARS            = 170   M1 bars for Ghost Watcher regime engine
```

---

## 7. Bot Details — SuperTrend v2.3

**Strategy:** M30 trend following with K-Means optimal factor selection.
K-Means runs over last 100 bars of vol-adjusted performance across all
factor variants, clusters into Best/Average/Worst — this is the genuine
innovation over fixed-parameter SuperTrend systems.

SuperTrend was effectively dormant in Week 1 (2 trades, both winners, but
n=2 is meaningless). Root cause not yet confirmed — session gate + conviction
gate + K-Means factor selection may be stacking into a too-narrow window.
This needs investigation before Gate 3 data can be collected.

### State machine
```
INCUBATING  waits incubation_bars_trending=2 M30 bars before active management
CONFIRMED   SI high, R-lock ratchet active, ST-line trailing
DECAYING    SI dropping, tighten trail, close after decaying_tolerance_bars
DEAD        close immediately (ST flip, SI collapse, ER churn)
```

### R-lock ratchet (CONFIRMED state)
```
+1R  SL to entry + 30pt buffer (BE lock)
+2R  SL to entry + 1x risk (lock 1R)
+3R  SL to entry + 2x risk (lock 2R)
```

### SI Score weights
```
si_weight_cluster  = 0.40
si_weight_regime   = 0.25
si_weight_adx      = 0.20
si_weight_volume   = 0.15
```

### Regime engine
M15 / 170 bars (approximately 1.77 days context). Fixed Jul 2026 — was
M1/170 bars (2.5 hours) which was the same mismatch class found for CAB.
Lower severity here since it only softens SI thresholds rather than
hard-gating an exit.

### Key parameters (Config defaults, some overridden per-symbol in config.json)
```
timeframe                    = M30
sl_multiplier                = 2.0  (per-symbol config: 2.5 to 3.5)
tp_safety_multiplier         = 5.0  (was 10.0 — never hit on M30 Gold)
max_positions                = 4    per symbol
incubation_bars_trending     = 2
si_confirmed                 = 0.65
si_decaying                  = 0.50
si_dead                      = 0.35
er_confirmed                 = 0.35
er_dead                      = 0.20
er_bars_for_dead             = 3
decaying_tolerance_bars      = 1
si_partial_close_min         = 0.65  (was 0.85, almost never fired before)
partial_close_profit_atr_mult = 1.5  (was 3.0, rarely reached)
equity_filter_enabled        = True
```

### Per-symbol config (from config.json — 11 active pairs)
```
EURUSDm  magic=101234  risk=1.0%  sl=2.5x  tp=5.0x  factors=1.0–3.5
GBPUSDm  magic=201567  risk=1.0%  sl=2.5x  tp=5.0x  factors=1.0–3.5
XAUUSDm  magic=301890  risk=1.0%  sl=2.5x  tp=5.0x  factors=1.5–3.5
XAGUSDm  magic=401213  risk=1.0%  sl=3.0x  tp=6.0x  factors=1.5–4.0
BTCUSDm  magic=402213  risk=0.5%  sl=3.0x  tp=6.0x  factors=1.0–3.5
ETHUSDm  magic=403213  risk=0.5%  sl=3.0x  tp=6.0x  factors=1.0–3.5
USTECm   magic=404213  risk=1.0%  sl=2.5x  tp=5.0x  factors=1.0–3.5
USOILm   magic=405213  risk=1.0%  sl=2.5x  tp=5.0x  factors=1.0–3.5
USDJPYm  magic=406213  risk=1.0%  sl=2.5x  tp=5.0x  factors=1.0–3.5
EURGBPm  magic=407213  risk=1.0%  sl=2.5x  tp=5.0x  factors=1.0–3.5
AUDNZDm  magic=408213  risk=1.0%  sl=2.5x  tp=5.0x  factors=1.0–3.5
```

---

## 8. Bot Details — CAB Watcher

**Strategy:** H4 two-bar inversion entry + fluid position management across
ALL symbols. Multi-symbol was a critical bug in Week 1 — only XAUUSDm
positions were ever managed. Fixed Jul 2026.

### Entry pattern (cab_entry.py)
```
Bullish: bar[-3] bearish AND bar[-2] bullish = BUY
Bearish: bar[-3] bullish AND bar[-2] bearish = SELL
```
- Bar-aware cache: fires once per H4 bar close
- Retry bound: max 3 order attempts per detected bar signal
  (was unlimited — retried every 30s for up to 4 hours on persistent failure)
- max_positions = 4 per symbol
- Lot sizing: ATR-based, capped at max_lot_demo_cap = 0.01
  (FX fills were 0.14-0.32 lot in Week 1 due to missing cap)
- Session gate: blocks 00:00-07:00 UTC
- Spread guard: blocks if spread > 50 points

### Management matrix (cab_watcher.py)

| Milestone | Condition | Action |
|---|---|---|
| PROTECTOR | R >= 1.2R | SL to BE + M15 ATR buffer, once per ticket |
| CONT_TIGHTEN | R > 0, post-protector | Light tighten 0.10xM15 ATR per cycle |
| HARVESTER | R >= 2.0R, any regime | Close 50%, trail remainder 1.5xM15 ATR |
| REAPER | R <= -0.5R, TRENDING | Triple gate: SI<0.45 AND ER<0.25 for 2+ cycles |
| REAPER suppress | Asian hours | 00:00-07:00 UTC on XAUUSD/XAUUSD.x |

### Regime engine
H4 / 50 bars (approximately 8 days context). Fixed from M1/170 bars (2.5h)
in Session 4 (audit-cab-tf). H4 entries need H4 regime context.

### Key constants
```
MAGIC_NUMBER           = 999555
TF_REGIME              = H4
REGIME_BARS            = 50
PROTECTOR_R            = 1.2
HARVESTER_R            = 2.0
REAPER_R               = 0.5
REAPER_SI_MAX          = 0.45
REAPER_ER_MAX          = 0.25
REAPER_ER_BARS         = 2
HARVESTER_PARTIAL_FRAC = 0.50
HARVESTER_TRAIL_ATR    = 1.5
MAX_FIRE_ATTEMPTS_PER_BAR = 3
```

---

## 9. Shared Infrastructure

### MT5 Gateway (mt5_gateway.py)
Single point of MT5 access. One threading.Lock. All bots use the same
gateway pattern — standalone fallback to raw mt5 if no gateway set.

```python
_GATEWAY = None
def set_gateway(gw): global _GATEWAY; _GATEWAY = gw
def _api(): return _GATEWAY if _GATEWAY is not None else mt5
```

IPC health: `_ipc_fail_count` increments on `symbol_info_tick()` None.
After 5 consecutive failures, `is_healthy` returns False, main thread
forces reconnect.

### SharedState (unified_runner.py)
In-memory store replacing CSV IPC. One RLock. Written by GhostWatcher,
read by GhostHunter. Also carries the circuit breaker flag.

```python
shared.conviction        # float 0-100
shared.adx               # float
shared.atr               # float
shared.regime            # RANGING / TRENDING_UP / TRENDING_DOWN / EXHAUSTION
shared.entries_allowed   # bool — circuit breaker gate
shared.daily_start_equity # sampled once at session start
```

### Circuit breaker
10% max daily loss. Blocks new entries across all bots. Watchers always
keep running — existing positions always managed regardless of the breaker.

### Live switches (config.json, no restart needed)
```json
"switches": {
    "supertrend_enabled": true,
    "cab_enabled": true,
    "ghost_grid": { "201": true, "202": true, "204": true }
}
```
Entries only — never management. Fail-open: if config.json is unreadable,
all switches default to enabled.

### MQL5 Failover (UnifiedFailover.mq5)
Single EA, all 9 magic numbers, one heartbeat file (cab_heartbeat.txt).
- Warn: 60s since last heartbeat
- Failover: 120s → 5xH1 ATR airbag trail on all positions
- Trail: only tightens, never widens existing SL
- ATR handles: cached per symbol (fixed Jul 2026 — was recreating handles
  every 5s cycle during the exact scenario where you want least failure surface)

---

## 10. Decision Gates — Current Status

| Gate | Description | Status |
|---|---|---|
| 1 | Unified runner stable — 4h smoke test, magic isolation clean | **PASSED** |
| 2 | Ghost Cache 204 vs 202 data decision — 30+ fills each | **IN PROGRESS** — Week 2 is first real data (204 fired zero times in Week 1 due to wiring bug, now fixed) |
| 3 | Individual bot conviction — ST avg R>0/30+ trades, CAB false-cut <30%, Ghost grid stop rate <20% | **IN PROGRESS** — Week 1 data severely compromised by structural bugs. Week 2 is the first clean run. |
| 4 | Regime complementarity — overlap <15%, combined drawdown lower than individual bots | Not yet evaluated |
| 5 | Bot-instrument fit matrix — avg_R by bot x instrument | Not yet evaluated |
| 6 | Live deployment — one process, micro-lot, avg R >0.3 over 50 live trades | **FINISH LINE** |

---

## 11. Development History

### Sessions 1-3 (April-May 2026) — Foundation built

- MT5Gateway thread-safe wrapper created
- All bots ported to gateway pattern
- Unified runner: 5 daemon threads, SharedState, magic isolation check
- CAB entry engine built from scratch (was MQL5-only)
- UnifiedFailover.mq5: single EA replacing 3 separate watchdogs
- Wilder ADX bug fixed across all 3 bots (was 14x too large — sum-init
  on the ADX smoothing step made all regime classifiers wrong)
- Ghost Cache (magic 204) built and unit-tested (10/10 pass)
- Per-personality leg TPs: 201=1.0R, 202=1.5R, 204=2.0R
  (was flat 0.5R for all — required 89% win rate just to break even,
  expectancy approximately -1.3R per trade)
- BE-lock ratchet at +1.0R for legs 202 and 204 (disabled for leg 201 — 
  1.0R TP and BE-lock at 1.0R are in direct conflict; see HIGH-5 comment 
  in manage_201_scalp)
- Emergency OOB TP: 3xATR broker-side failsafe on Fluid TP legs
- CAB regime engine: M1 to H4 (8 days structural context)
- Gate 1 PASSED: unified smoke test clean

### Session 4 (May 2026) — Profit-booking bugs fixed

- CAB tick=None crash fixed (silently swallowed profitable closes)
- CAB processed_actions blanket reset fixed (wiped HARVESTER/PROTECTOR
  flags on any reconnect — made positions look new each cycle)
- CAB HARVESTER regime lock removed (was EXHAUSTION-only, trades at +2R
  in STABLE/TRENDING were never harvested)
- ST partial close thresholds corrected: SI gate 0.85 to 0.65, ATR
  multiplier 3.0 to 1.5 (original values almost never triggered)
- ST R-lock ratchet added: BE at +1R, lock 1R at +2R, lock 2R at +3R
- Position caps raised: ST and CAB max_positions 1 to 4 per instrument
- Global position cap removed, replaced by daily equity circuit breaker
- Architecture decision: per-instrument caps + circuit breaker is the
  correct dual-layer risk control, not a global position count

### Week 1 Demo Run (July 5-17 2026) — First real data

Account: $10,000 → $7,739.28 (-$2,260.72, -22.6% in 12 days)

**Results by strategy:**
```
Ghost Grid 201/202/203  2,507 trades  P&L -$408.70  Win% 55.0%
  201 SCALP    1,373 trades  -$184.67  Win% 55.3%  Avg/trade -$0.135
  202 REVERSAL 1,085 trades  -$135.52  Win% 55.7%  Avg/trade -$0.125
  203 TREND       49 trades   -$88.51  Win% 32.7%  Avg/trade -$1.806
CAB all symbols    18 closed  P&L -$1,846.79  Win% 0.0%
SuperTrend          2 trades  P&L +$4.47     Win% 100%
```

**The single most important finding — BE-lock cohort split:**
```
Reached +0.5R BE-lock milestone:   n=1,438  P&L +$2,239  Win% 94.4%
Never reached BE-lock (SL first):  n=1,020  P&L -$2,560  Win%  0.5%
```
41.5% of 201/202 trades died before reaching +0.5R. The 0.2xATR SL was
inside a single M1 bar's normal noise on Gold. This one cohort caused
more loss than the entire Ghost Grid total for the period.

**Ghost Grid grid mechanism was completely non-functional:**
```
GRID TP fired             0 times (5 weeks of logs)
GRID STOP fired           0 times
GRID PROTECTOR fired      0 times
LEG_TP SET                8 times  (should be approximately 2,500)
Depth cap refused       47,672 log lines
GridState reset           0 times
```
Root cause: the `_cap_refused` gating in manage_201/202/204 excluded
cap-refused tickets from `_set_leg_tp()`. GridState filled on day 1
and never reset. Every position after the first 4 legs had no TP, no
grid tracking, and no grid-level exit for the entire test window.

**CAB was completely broken for non-Gold positions:**
`manage_fluid_logic()` used `positions_get(symbol=SYMBOL)` hardcoded to
XAUUSDm. Every EUR/GBP/XAG position received zero management — no
PROTECTOR, HARVESTER, or REAPER ever fired. Every closed CAB trade was
a SL hit. This explains the 0% win rate and -$1,846.79 loss (82% of
total drawdown).

**Session analysis (Ghost Grid, all legs):**
```
ASIAN      1,045 trades  P&L +$15.87   Win% 58.8%   only profitable session
LONDON       472 trades  P&L -$52.60   Win% 55.9%
NY_OVERLAP   426 trades  P&L -$161.19  Win% 56.3%
NY_CLOSE     564 trades  P&L -$210.78  Win% 46.3%   worst session
```
Counter-intuitive result: Asian session profitable, NY_CLOSE the worst.
Session filtering is not being applied — all sessions stay open until
clean data from a properly-functioning system is available.

### July 2026 — Full fix batch (Tier 0, Tier 1, Tier 3, audit)

**Mechanical bugs fixed (Tier 0 — most critical):**

Fix 1 — Ghost Grid TP and reset (sniper_watcher.py):
Every 201/202/204 leg now gets personality TP set unconditionally on
registration, decoupled from the 4-leg grid cap. _set_leg_tp() is
idempotent so calling it unconditionally is safe. 72-hour staleness
timeout added: if a grid sits open without hitting TP/stop, force-close
remaining legs and reset. This was the root cause of LEG_TP_SET firing
8 times in 5 weeks.

Fix 2 — CAB multi-symbol (cab_watcher.py):
manage_fluid_logic() now fetches all magic-999555 positions with no
symbol filter. Regime and M15 ATR computed once per distinct symbol
present each cycle. Every close/modify uses pos.symbol. _modify_sl()
also got the None-guard for tick/sym_info that _close_position already
had. Behavior-tested: confirmed all 3 non-Gold symbols independently
evaluated where previously only XAUUSDm was touched.

Fix 3 — Ghost Cache wiring (unified_runner.py, ghost_sniper.py):
GhostCache now imported and instantiated on both UP_PROBE and DOWN_PROBE
arming in the live hunter thread. update() called every tick. ghost_cache
reset alongside armed on every path that clears armed. Also fixed the
same missing import in ghost_sniper.py standalone run_hunter().
Tested: GhostCache object fires correctly end-to-end, import chain
verified through the same path the live thread uses.

**Parameter change (Tier 1):**

Fix 4 — Wider SL for 201/202 (ghost_sniper.py, unified_runner.py):
SCALP_REV_SL_ATR_MULT = 0.35 (was 0.2). Single named constant read by
both files — they can no longer drift apart. First test point, not a
final answer — re-measure after Week 2.

**Architecture (Tier 3):**

Fix 5 — Regime gate on probe arming (unified_runner.py):
GHOST_ARM_BLOCKED_REGIMES blocks new probe arming during TRENDING_UP,
TRENDING_DOWN, UNKNOWN. In-flight probes run to natural exit regardless
of regime drift mid-probe.

Fix 6 — Global per-bot/per-leg switch (config.json, unified_runner.py):
load_switches() reads config.json on every cycle. Entries only, never
management. Fails open to all-enabled on any error.

**Audit fixes:**

Fix A — ST test suite (test_supertrend_gateway_port.py):
Was importing supertrend_bot_v2_3 which does not exist — ModuleNotFoundError.
Test suite had never run against real code. Fixed to import core.supertrend_bot.
Now 12/12 pass.

Fix B — CAB retry storm (cab_entry.py):
MAX_FIRE_ATTEMPTS_PER_BAR = 3. Previously retried order placement every
30s for up to 4 hours on a persistent failure. Reproduced the bug (5
consecutive failed cycles), confirmed bounded (3 attempts then stop).

Fix C — CAB demo lot cap (cab_entry.py, config.json):
max_lot_demo_cap = 0.01. FX fills were 0.14-0.32 lot in Week 1 because
risk-based sizing had no ceiling beyond the broker volume_max.

Fix D — ST regime window (core/supertrend_bot.py):
Regime engine M1/170 bars (2.5h) changed to M15/170 bars (approximately
1.77 days). Same mismatch class as the CAB H4 fix, lower severity.

Fix E — Magic isolation detail logging (unified_runner.py):
MAGIC_ISOLATION_BREACH_DETAIL now includes magic number and symbol in
the structured CSV. Previously only a count was logged.

Fix F — verify_structure.py duplicate:
cab_candidates = ["cab_watcher.py", "cab_watcher.py"] had a harmless
duplicate entry. Cleaned up.

Fix G — ATR handle caching (UnifiedFailover.mq5):
Was calling iATR() + CopyBuffer() + IndicatorRelease() fresh every
5-second airbag cycle for every managed position. Now caches one handle
per symbol, releases all on OnDeinit.

Fix H — run_bot.py standalone warning:
Explicit warning added to docstring and startup banner — never run
alongside unified_runner.py against the same MT5 terminal.

Fix I — Credential env-var fallback (unified_runner.py):
MT5_LOGIN, MT5_PASSWORD, MT5_SERVER, MT5_PATH environment variables
override config.json when set. Fully backward compatible.

**Test suite status (current):**
```
test_cab_entry.py              18/18 PASS  (T17/T18 added for CAB restart fix)
test_cab_gateway_port.py       10/10 PASS
test_ghost_gateway_port.py     17/17 PASS  (T14–T17 added for per-magic GridState)
test_supertrend_gateway_port.py 12/12 PASS
```

### August 2026 — Terminal hijack fix

- **What happened:** A pathless `mt5.initialize(timeout=30000)` call inside the `SuperTrend` reconnect handler caused the bot to hijack the first available terminal on the system (the CAB standalone terminals) during brief connection drops. It executed a login with V1 credentials (474167713), forcefully added the V1 account to the CAB terminals, and began firing SuperTrend trades on the wrong terminals.
- **Files fixed:** `core/supertrend_bot.py` (rogue reconnect logic removed), `cab_multi_pair/connection.py` (pathless fallback removed).
- **Principle locked:** No bot module is ever permitted to call `mt5.initialize()` directly. `MT5Gateway` exclusively owns all MT5 initialization and reconnect logic, locking all bots strictly to their configured `TERMINAL_PATH`. This has been verified clean across the full codebase.
---

## 12. Open Items — Deferred by Design

These are decisions made deliberately, not forgotten.

**Tier 2 — Hold until Week 2 data in hand:**
- BE-lock step ratchet: replace the one-shot BE-lock with a progressive
  step (+0.5R→BE, +1.0R→lock 0.3R, +1.5R→lock 0.7R). Currently 73% of
  BE-locked trades cap out at scratch because there is no step-up past
  the initial lock. Deliberately held until the TP bug and wider SL
  changes are measured cleanly first.

**Session filtering — parked until clean baseline:**
- Asian session was the only profitable Ghost Grid session in Week 1.
  NY_CLOSE was the worst. But this data came from a broken system (no
  TP, wrong SL size). Applying session filters now would just hide which
  session masks the bug best. All sessions stay open in Week 2.

**Leg 203 — Officially Removed:**
- Ghost Grid's Leg 203 (Trend-follow) has been completely retired and handed over to SuperTrend. Any lingering config switches or logic for 203 should be ignored/purged. SuperTrend is now the exclusive trend-following engine.

**Controlled grid-sizing / martingale:**
- Discussed in July 2026. Rejected for now: the grid tracking mechanism
  had to be confirmed working first (it was not in Week 1). Rules when
  this is revisited: fixed lot per leg, hard leg cap, explicit
  worst-case calculated before first leg. Not before Gates 2 and 3
  have clean data behind them.

**Market Exit Score (Phase 3):**
After Phase 2 data collection, replace static R-threshold exits with a
continuous 0-1 score. Components: regime alignment 30%, conviction trend
25%, ATR trajectory 20%, directional momentum 15%, opposite signal
quality 10%. Not started.

**Bot-instrument fit matrix (Gate 5):**
Which bot runs which pair on live is decided from avg_R cross-tab data,
not assumed. Needs Phase 2 data from a functioning system.

**Prop firm challenge:**
Explicitly deferred to post-development.

**Unknown trades investigation:**
`WATCHER_BUY`, `SENTINEL_INVALID`, `PARTIAL_SCALE` comment tags on
trades for USDJPYm/EURGBPm. Something external is trading this account.
Week 2 magic isolation breach detail logging should identify the culprit.

---

## 13. What to Watch in Week 5 (Statistical Gate Evaluation)

**Priority checks (daily):**
- 204 fires: grep "GHOST_CACHE_FIRE" logs/sniper_hunter.log — expect 
  first fires this week after decouple fix. If zero after 5 trading 
  days, lower N_LAYERS_DEFAULT to 2 in ghost_cache.py.
- CAB log active: confirm logs/cab_watcher.log has content within first 
  hour. If empty, decouple fix deployed but log path fix did not — 
  re-check cab_watcher.py RotatingFileHandler path on VPS.
- H6 gate logging: after F6 implementation, check for 
  GHOST_ARM_BLOCKED_ST_LONG in logs/sniper_hunter.log or 
  logs/unified_runner.log. Confirms the cross-bot gate is firing.
- All 5 threads alive: grep "Heartbeat OK" logs/unified_runner.log.
- No FILL_NO_SL lines: grep "FILL_NO_SL" logs/sniper_hunter.log 
  should return zero (SLTP fix holding).

**End-of-week analysis targets:**
- Session × H4 direction breakdown for Week 5 Ghost 202 fills — compare 
  to Week 4 findings. If LONDON win rate, NY_CLOSE+H4_UP win rate, and 
  ATR Q2 win rate all hold within 3pp of Week 4 values, statistical 
  gates are confirmed and ready for implementation in Week 6.
- CAB H5 SL distance calculation from logs/cab_watcher.log.
- 204 fire count and depth distribution (max_layer_touched at fire time) 
  — validates whether n=3 is correct or should be lowered.
- SuperTrend symbol P&L — confirm XAUUSDm continues negative R. If three 
  consecutive negative weeks, disable via config switch (magic 301890 
  supertrend_enabled=false for XAUUSDm) entering Week 6.

**Files to collect for end-of-week review:**
- logs/sniper_v51_live_audit.csv (same columns as Week 4)
- logs/sniper_hunter.log (check for GHOST_CACHE_FIRE and 
  GHOST_ARM_BLOCKED_ST_LONG)
- logs/unified_runner.log
- logs/cab_watcher.log (NEW — first week this file is active)
- logs/kpi_ledger.csv
- MT5 ReportHistory HTML export (full account, all magic numbers, 
  Aug 16–22 2026)
- Output of end-of-week statistical gate query (run same ghost202 
  session×H4 analysis script against combined Week 4+5 data)

## 14. Architectural Principles — Non-Negotiable

1. A switch or gate can never stop position management. Only entry
   creation is gatable. This is the root lesson of the CAB bug.

2. R-multiples measure outcomes. They must not mechanically trigger exits.
   Market state drives exit decisions.

3. Ghost Grid MAX_LEGS = 4 is non-negotiable during demo. Not 5, not 6.
   Evaluate statistically after 50+ grids, not impressionistically.

4. Sequential same-direction entries are directional confirmation, not
   compounding risk. This justifies per-instrument caps of 4.

5. Daily equity circuit breaker is the correct global safety mechanism.
   Per-instrument caps handle instrument exposure. Do not double-layer
   with a global position count cap.

6. Parameter changes go one at a time, measured against a clean baseline.
   The SL width change (0.2 to 0.35xATR) is the only parameter change in
   Week 2. Everything else waits for this measurement to be clean.

7. Demo execution is not live execution. The gap is largest for Ghost Grid
   (M1, execution-sensitive). More live micro-lot time needed for Ghost
   Grid specifically before scaling.

8. Never increase grid depth cap or widen grid stop during demo testing.
   Evaluate both statistically after 50+ grids.

9. **[PERMANENT] No arbitrary trade filtering.** The Knowledge Register
   and Order Router must never block trades based on unvalidated
   assumptions. Filters are only introduced after statistical analysis
   of raw logging data proves which market behaviors/regimes produce
   winners vs losers. Until that analysis is complete, every generated
   signal flows to the market unobstructed.

---

## 15. Live Deployment Sequencing (from master plan, unchanged)

When Gates 3, 4, and 5 are passed:

1st: SuperTrend (with CAB watchdog attached)
Gate: Pass Phase 2 + manual failover test confirmed
Scale: 30 live trades

2nd: CAB Watcher (upgraded, SI-gated)
Gate: Demonstrated synergy and proper regime synchronization with ST

3rd: Ghost Grid (grid watcher confirmed functional)
Gate: 50+ grids with grid stop firing < 20% of grids

**Important Note on Expectancy:**
During the demo/raw-logging phase, forcing a purely positive expectancy metric is a distraction. The goal is **sync among bot levels**. If the bots are correctly synchronized across their respective regimes (hedging each other during transitions and capturing their target behaviors), positive expectancy will be the automatic mathematical byproduct.

All at micro-lot (0.01) throughout initial live period before any scaling.

---

## 16. Running the Bot

```bash
# Recommended — always use this
python unified_runner.py --account demo

# After any file change, before deploying
python verify_structure.py

# Before deploying any code change
python test_cab_entry.py
python test_cab_gateway_port.py
python test_ghost_gateway_port.py
python test_supertrend_gateway_port.py

# After a week of data collection
python extract_bot_logs.py --days 7

# LEGACY ONLY — never alongside unified_runner.py on same terminal
python run_bot.py --account demo --symbols XAUUSDm
```

This document is the single authoritative reference. Update it after each
major session, parameter change, or gate decision. The old UNIFIED_BOT_CONTEXT.md
is superseded by this version.
